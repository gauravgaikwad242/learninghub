import { TopicTitleDirective } from './topic-title.directive';

describe('TopicTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new TopicTitleDirective();
    expect(directive).toBeTruthy();
  });
});
