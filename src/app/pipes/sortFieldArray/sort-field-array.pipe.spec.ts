import { SortFieldArrayPipe } from './sort-field-array.pipe';

describe('SortFieldArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFieldArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
